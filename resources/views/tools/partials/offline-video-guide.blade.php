<p class="tool-hint">{{ __('tools.offline_hint') }}</p>

<div class="tool-panel">
    <div class="offline-grid">
        <section class="offline-card">
            <h3>{{ __('tools.offline_app_title') }}</h3>
            <p>{{ __('tools.offline_app_text') }}</p>
        </section>

        <section class="offline-card">
            <h3>{{ __('tools.offline_premium_title') }}</h3>
            <p>{{ __('tools.offline_premium_text') }}</p>
        </section>

        <section class="offline-card">
            <h3>{{ __('tools.offline_creator_title') }}</h3>
            <p>{{ __('tools.offline_creator_text') }}</p>
        </section>

        <section class="offline-card">
            <h3>{{ __('tools.offline_platform_title') }}</h3>
            <p>{{ __('tools.offline_platform_text') }}</p>
        </section>
    </div>

    <p class="tool-note">{{ __('tools.offline_note') }}</p>
</div>
